# عنوان درست است

فایل config.json را باز کن.

config.json را باز کن.

- فایل install.ps1 را اجرا کن.
- install.ps1 را اجرا کن.

This is a plain English line with no Persian at all.

The word فارسی appears once in this otherwise long English sentence here.

‏config.json با کاراکتر نامرئی شروع شده است.

```bash
config.json را داخل بلوک کد نباید بررسی کند
```

[README.md](README.md) را بخوان.

۱۲۳ فایل در پوشه هست.
